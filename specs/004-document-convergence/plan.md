# 技术方案

文件级盘点与内容审查分开记录。先识别镜像、参考、手册、历史和混合计划，再检查需求去向。复核记录见 contracts/document-audit.md 和 contracts/inventory.json。

编号 002/003 曾用于已撤销的功能，本轮使用 004 起的新编号；不恢复 NUC dsh Web 入口，也不把 Clip 2 分钟过期重新排为实施任务。

## 宪法检查

PC 本地维护和构建；NUC 仅镜像。文档整理不授权系统切换、应用发布、密钥轮换或实机操作。


## 本机规则安装

现有 Home Manager 没有工作区规则入口，/data/project 也不属于 home.file 默认范围。
使用 scripts/agent-rules.py 和 just agent-rules，固定四个文件映射，先全面检查再安装
符号链接；同一源避免使用副本漂移，不修改系统配置。模拟临时目录验证差异保护、
错误链接/父路径拒绝、预演无写入、重复安装和源修改生效；实际安装后 --check 核验。
配置属于本仓库，原产品规格归属不变；现有远端同步只写 nix-tools 自己的白名单。
